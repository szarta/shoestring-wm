research/
target/
docs/_build/

# Local task tracker (todo-sqlite-cli) — user-local, not project state.
.todo-sqlite-cli
todo.sqlite

# Scratch notes file.
test

# WLCS runner output (generated by tests/wlcs/run-wlcs.sh).
tests/wlcs/results.txt

# Build artifacts from the IPC client libraries (clients/).
clients/typescript/node_modules/
clients/typescript/dist/
clients/typescript/package-lock.json

# Python bytecode cache (e.g. from running the example).
clients/python/__pycache__/
