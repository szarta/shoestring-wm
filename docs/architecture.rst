Architecture
============

The architecture document lives in Markdown at the repository root:
:file:`docs/architecture.md`. It covers the state model, window
storage, workspace semantics, output management, and the milestone
plan that drove the v1 implementation.

(A future revision of this guide will translate it into RST in-place.)
