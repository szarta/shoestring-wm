# WLCS flaky-list — tests that pass some runs and fail others. Listed tests are
# exempt from the gate in BOTH directions (neither a pass nor a fail is a
# regression). See check-results.py.
#
# Pinned suite: WLCS v1.8.1.
#
# !! INCOMPLETE BY NATURE !! These are the KNOWN members of a broad LOAD/timing-
# dependent band found by aggregating 23 serial runs during task-87 seeding. They
# pass on an idle machine and fail more under load: each hinges on an async
# configure->ack->resize / commit round-trip racing the test's pointer/position
# check, which the headless harness paces on a ~16ms render-loop tick. The band's
# membership SHIFTS run-to-run, so this list cannot be exhaustively enumerated by
# repeated runs — a *blocking* gate needs deterministic harness frame pacing
# (follow-up task), not a longer flaky-list. All members sit in already-xfail
# smithay-baseline buckets (layer-shell 133 / input-region 132 / pointer-
# constraints / subsurface 153 / viewporter), so excluding them costs no real
# conformance signal. Until the harness fix lands, WLCS CI stays INFORMATIONAL
# (non-blocking).

Anchor/LayerSurfaceLayoutTest.is_initially_positioned_correctly_for_anchor/2  # layer-shell arrange/resize timing (task 133)
Anchor/LayerSurfaceLayoutTest.is_positioned_correctly_after_multiple_changes/20  # layer-shell arrange/resize timing (task 133)
Anchor/LayerSurfaceLayoutTest.is_positioned_correctly_after_multiple_changes/29  # layer-shell arrange/resize timing (task 133)
Anchor/LayerSurfaceLayoutTest.is_positioned_correctly_after_multiple_changes/3  # layer-shell arrange/resize timing (task 133)
Anchor/LayerSurfaceLayoutTest.is_positioned_correctly_when_layout_changed/0  # layer-shell arrange/resize timing (task 133)
Anchor/LayerSurfaceLayoutTest.is_positioned_correctly_when_layout_changed/13  # layer-shell arrange/resize timing (task 133)
Anchor/LayerSurfaceLayoutTest.is_positioned_correctly_when_layout_changed/3  # layer-shell arrange/resize timing (task 133)
Anchor/LayerSurfaceLayoutTest.maximized_xdg_toplevel_is_shrunk_for_exclusive_zone/12  # layer-shell arrange/resize timing (task 133)
Anchor/LayerSurfaceLayoutTest.maximized_xdg_toplevel_is_shrunk_for_exclusive_zone/7  # layer-shell arrange/resize timing (task 133)
Anchor/LayerSurfaceLayoutTest.surfaces_with_exclusive_zone_set_to_negative_one_do_not_respect_other_exclusive_zones/28  # layer-shell arrange/resize timing (task 133)
Anchor/LayerSurfaceLayoutTest.surfaces_with_exclusive_zone_set_to_negative_one_do_not_respect_other_exclusive_zones/30  # layer-shell arrange/resize timing (task 133)
Anchor/LayerSurfaceLayoutTest.surfaces_with_exclusive_zone_set_to_negative_one_do_not_respect_other_exclusive_zones/5  # layer-shell arrange/resize timing (task 133)
MultiRectEdges/RegionSurfaceInputCombinations.input_inside_region_seen/22  # input-region boundary hit-test timing (task 132)
PointerConstraints.can_get_locked_pointer  # pointer-constraints commit/activation timing
PointerConstraints.confined_pointer_does_move  # pointer-constraints commit/activation timing
SurfaceInputRegions/SurfaceInputCombinations.input_falls_through_subsurface_when_parent_unmapped/10  # input-region boundary hit-test timing (task 132)
SurfaceInputRegions/SurfaceInputCombinations.input_hits_parent_after_falling_through_subsurface/10  # input-region boundary hit-test timing (task 132)
SurfaceInputRegions/SurfaceInputCombinations.input_not_seen_over_empty_region/10  # input-region boundary hit-test timing (task 132)
WpViewporterTest.committing_new_destination_without_new_buffer_still_changes_size  # viewporter destination commit-timing race
XdgShellStableSubsurfaces/SubsurfaceMultilevelTest.subsurface_with_sync_parent_does_not_move_when_only_parent_committed/0  # subsurface commit-timing (task 153)
